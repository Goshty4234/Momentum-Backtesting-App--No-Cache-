# Core dependencies for Momentum Backtesting App
streamlit>=1.28.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
plotly>=5.15.0
yfinance>=0.2.18
scipy>=1.10.0
pandas-market-calendars>=4.3.0

# Optional dependencies (for enhanced features)
mplcursors>=0.5.0  # For interactive matplotlib cursors
streamlit-plotly-events>=0.1.0  # For interactive plotly events

# Note: The app will work without the optional dependencies,
# but some interactive features may be disabled.
